{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "request": {
      "type": "object",
      "properties": {"status": {"enum": ["pending", "blocked"]}},
      "required": ["id", "status", "acknowledged"]
    }
  },
  "required": ["request"]
}
